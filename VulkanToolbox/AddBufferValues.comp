#version 460

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer Data {
	uint ints[];
} intData;

void main()
{
	uint gID = gl_GlobalInvocationID.x+1;
	
	if (gID < intData.ints[0])
	{
		intData.ints[gID]++;
	}
}